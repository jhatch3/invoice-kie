# invoice-kie — Python dependencies for the CORD document-KIE benchmark.
# Install with:  pip install -r requirements.txt

# --- Data (Phase 1: load + store CORD locally) ---
datasets>=2.18
huggingface_hub>=0.23
pandas>=2.2
pyarrow>=15
Pillow>=10.0

# --- Modeling (Phase 2: fine-tune LayoutLMv3) ---
torch>=2.2
transformers>=4.40
accelerate>=0.29
seqeval>=1.2.2          # entity-level F1 for token classification
evaluate>=0.4

# --- Zero-shot VLM baseline (Phase 3: Qwen2-VL) ---
qwen-vl-utils>=0.0.8

# --- OCR (only for running the pipeline on new PDFs; CORD ships word boxes) ---
# pytesseract>=0.3.10   # requires system Tesseract
# pdf2image>=1.17       # requires system poppler

# --- Dev / testing ---
pytest>=8.0
ruff>=0.4
